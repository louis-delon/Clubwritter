<div class="container">

<% @themes.each do |theme| %>
  <div class=row>
    <h1><%= link_to "#{theme.name}", theme_posts_path(theme) %></h1>
    <%= theme.user.pseudo %><br>
    <%= theme.created_at %><br>
  </div>
<% end %>

</div>
